# cppcheck suppressions list
# Each line is a suppression pattern used by cppcheck CLI --suppressions-list
# Format examples:
# path:warningId
# src/somefile.cpp:unusedFunction
# */generated/*:missingInclude

# Add project-specific suppressions below (one per line)

# Suppress missingIncludeSystem warnings - cppcheck doesn't need system headers for proper analysis
missingIncludeSystem

# Suppress syntaxError for complex macro definitions in cppcheck_defines.txt
# These macros are essential for the project logic and cannot be modified
syntaxError:cppcheck_defines.txt

